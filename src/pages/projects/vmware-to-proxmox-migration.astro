---
import ProjectLayout from "../../layouts/ProjectLayout.astro";

const frontmatter = {
  title: "VMware to Proxmox Migration",
  status: "Completed",
  statusType: "completed",
  origin: "Infrastructure Project",
  date: "2025.08",
  techStack: ["Proxmox VE", "NETLAB+", "10G Networking", "Linux", "KVM/QEMU"],
};

// Cost comparison data (based on Broadcom's $350/core/year VCF pricing)
// 196 cores × $350 = $68,600/year
const costComparison = {
  vmware: {
    annual: "~$68,600",
    fiveYear: "~$343,000",
    model: "Per-core subscription",
    minimum: "72-core minimum",
  },
  proxmox: {
    annual: "$0",
    fiveYear: "$0",
    model: "Open source (AGPLv3)",
    minimum: "None",
  }
};

// Citation sources
const sources = [
  { id: "sdxcentral", title: "SDxCentral: VMware Price & License Changes Analysis", url: "https://www.sdxcentral.com/articles/analysis/whats-up-broadcoms-vmware-price-license-changes/2024/06/" },
  { id: "cloudtango", title: "CloudTango: VMware Price Increase - What You Need to Know", url: "https://www.cloudtango.net/blog/2025/11/03/vmware-price-increase-what-you-need-to-know/" },
  { id: "rimini", title: "Rimini Street: VMware License Renewal Cost Estimator", url: "https://www.riministreet.com/vmware-license-renewal-cost-estimator/#calculator" },
  { id: "ndg", title: "NDG: Termination of VMware IT Academy", url: "https://support.netdevgroup.com/hc/en-us/articles/25519276731543-Termination-of-VMware-IT-Academy-by-Broadcom" },
  { id: "techtarget", title: "TechTarget: Broadcom axes VMware education licensing", url: "https://www.techtarget.com/searchdatacenter/news/366605408/Broadcom-axes-VMware-education-licensing-options-for-schools" },
  { id: "computerweekly", title: "Computer Weekly: Education sector facing huge VMware cost increases", url: "https://www.computerweekly.com/news/366579593/Education-sector-facing-huge-VMware-cost-increases-after-Broadcom-ends-discounts" },
  { id: "idc", title: "IDC: VMware Cost Increases", url: "https://blogs.idc.com/2024/08/07/vmware-cost-increases-how-broadcom-vmware-product-offerings-are-evolving/" },
];

// Server cluster specs (sanitized)
const clusterSpecs = {
  nodes: 6,
  totalCores: 196,
  totalRam: "2.75 TB",
  model: "Lenovo ThinkSystem SR630",
};

// Project phases
const phases = [
  { phase: 1, title: "Assessment & Planning", status: "complete", tasks: ["Hardware inventory", "Cost analysis", "Network design", "Timeline planning"] },
  { phase: 2, title: "Hardware Installation", status: "complete", tasks: ["10G PCIe NIC installation (all 6 servers)", "Cable infrastructure deployment", "Storage expansion"] },
  { phase: 3, title: "Proxmox Deployment", status: "complete", tasks: ["OS installation on all hosts", "Driver verification", "Initial configuration"] },
  { phase: 4, title: "Network Configuration", status: "complete", tasks: ["LACP bonding setup", "VLAN configuration", "10G management network", "Bridge configuration"] },
  { phase: 5, title: "Cluster Formation", status: "complete", tasks: ["6-node cluster creation", "Quorum establishment", "Storage pool configuration"] },
  { phase: 6, title: "NETLAB+ Integration", status: "complete", tasks: ["NDG post-install optimization", "VM migration", "Lab pod deployment", "Student access provisioning"] },
];

// Skills gained
const skillsGained = [
  { category: "Virtualization", items: ["Proxmox VE administration", "KVM/QEMU hypervisor", "LXC containers", "Live migration"] },
  { category: "Networking", items: ["10G infrastructure", "LACP bonding (802.3ad)", "VLAN trunking", "Linux bridges"] },
  { category: "Infrastructure", items: ["Enterprise server hardware", "PCIe NIC installation", "Storage architecture", "Cluster management"] },
  { category: "Documentation", items: ["Network topology mapping", "Port documentation", "Runbook creation", "Change management"] },
];
---

<ProjectLayout frontmatter={frontmatter}>
  <h2>// Overview</h2>
  <p>
    Led the complete infrastructure migration of our educational lab environment from VMware ESXi to Proxmox VE.
    This wasn't a team effort with delegated tasks; I was the workhorse. I physically installed hardware, configured
    every network port, built the cluster, broke it, rebuilt it, documented everything, and delivered a production-ready
    platform. The project eliminated six figures in annual licensing costs while modernizing the entire network architecture.
  </p>

  <!-- The Crisis -->
  <h2>// The Catalyst: Broadcom's VMware Acquisition</h2>
  <p class="section-intro">
    In November 2023, Broadcom completed its $61 billion acquisition of VMware and immediately began dismantling
    the licensing models that educational institutions depended on:
  </p>

  <div class="crisis-timeline">
    <div class="crisis-event">
      <div class="event-marker bad"></div>
      <div class="event-content">
        <span class="event-date">Nov 2023</span>
        <span class="event-title">Broadcom acquires VMware</span>
        <span class="event-desc">$61B acquisition closes; licensing changes announced within weeks</span>
      </div>
    </div>
    <div class="crisis-connector"><div class="connector-particle"></div></div>
    <div class="crisis-event">
      <div class="event-marker bad"></div>
      <div class="event-content">
        <span class="event-date">Early 2024</span>
        <span class="event-title">Perpetual licenses discontinued</span>
        <span class="event-desc">Subscription-only model; per-core pricing replaces per-socket</span>
      </div>
    </div>
    <div class="crisis-connector"><div class="connector-particle"></div></div>
    <div class="crisis-event">
      <div class="event-marker bad"></div>
      <div class="event-content">
        <span class="event-date">Apr 2024</span>
        <span class="event-title">72-core minimum requirement</span>
        <span class="event-desc">Small deployments forced to license 72 cores minimum per product</span>
      </div>
    </div>
    <div class="crisis-connector"><div class="connector-particle"></div></div>
    <div class="crisis-event">
      <div class="event-marker bad"></div>
      <div class="event-content">
        <span class="event-date">Aug 2024</span>
        <span class="event-title">VMware IT Academy terminated</span>
        <span class="event-desc">Free educational licensing completely eliminated</span>
      </div>
    </div>
    <div class="crisis-connector"><div class="connector-particle"></div></div>
    <div class="crisis-event">
      <div class="event-marker good"></div>
      <div class="event-content">
        <span class="event-date">Aug 2025</span>
        <span class="event-title">Migration completed</span>
        <span class="event-desc">Full Proxmox deployment operational; zero VMware dependency</span>
      </div>
    </div>
  </div>

  <!-- Cost Savings -->
  <h2>// The Numbers: Cost Avoidance</h2>
  <p class="section-intro">
    The financial impact of staying on VMware was incompatible with educational IT budgets:
  </p>

  <div class="cost-comparison">
    <div class="cost-card vmware">
      <div class="cost-header">
        <span class="cost-icon">💸</span>
        <span class="cost-label">VMware (Broadcom)</span>
      </div>
      <div class="cost-body">
        <div class="cost-row">
          <span class="cost-key">Annual Cost</span>
          <span class="cost-value bad">{costComparison.vmware.annual}</span>
        </div>
        <div class="cost-row">
          <span class="cost-key">5-Year TCO</span>
          <span class="cost-value bad">{costComparison.vmware.fiveYear}</span>
        </div>
        <div class="cost-row">
          <span class="cost-key">Model</span>
          <span class="cost-value">{costComparison.vmware.model}</span>
        </div>
        <div class="cost-row">
          <span class="cost-key">Minimum</span>
          <span class="cost-value">{costComparison.vmware.minimum}</span>
        </div>
      </div>
    </div>

    <div class="cost-arrow">
      <div class="arrow-savings">
        <span class="savings-label">ANNUAL SAVINGS</span>
        <span class="savings-value">~$68,600</span>
      </div>
    </div>

    <div class="cost-card proxmox">
      <div class="cost-header">
        <span class="cost-icon">✓</span>
        <span class="cost-label">Proxmox VE</span>
      </div>
      <div class="cost-body">
        <div class="cost-row">
          <span class="cost-key">Annual Cost</span>
          <span class="cost-value good">{costComparison.proxmox.annual}</span>
        </div>
        <div class="cost-row">
          <span class="cost-key">5-Year TCO</span>
          <span class="cost-value good">{costComparison.proxmox.fiveYear}</span>
        </div>
        <div class="cost-row">
          <span class="cost-key">Model</span>
          <span class="cost-value">{costComparison.proxmox.model}</span>
        </div>
        <div class="cost-row">
          <span class="cost-key">Minimum</span>
          <span class="cost-value">{costComparison.proxmox.minimum}</span>
        </div>
      </div>
    </div>
  </div>

  <div class="savings-highlight">
    <div class="highlight-stat">
      <span class="stat-value">~$343,000</span>
      <span class="stat-label">Five-year cost avoidance (196 cores × $350/core × 5 years)</span>
      <a href="#sources" class="cite-link">See sources below →</a>
    </div>
  </div>

  <!-- Infrastructure -->
  <h2>// Infrastructure Architecture</h2>
  <p class="section-intro">
    The migration involved a 6-node enterprise server cluster running educational lab environments for NETLAB+:
  </p>

  <div class="cluster-diagram">
    <div class="cluster-header">
      <span class="cluster-name">PVE-NETLAB Cluster</span>
      <span class="cluster-status"><span class="status-dot"></span> Operational</span>
    </div>

    <div class="cluster-stats">
      <div class="cluster-stat">
        <span class="stat-num">{clusterSpecs.nodes}</span>
        <span class="stat-label">Nodes</span>
      </div>
      <div class="cluster-stat">
        <span class="stat-num">{clusterSpecs.totalCores}</span>
        <span class="stat-label">CPU Cores</span>
      </div>
      <div class="cluster-stat">
        <span class="stat-num">{clusterSpecs.totalRam}</span>
        <span class="stat-label">Total RAM</span>
      </div>
    </div>

    <div class="cluster-nodes">
      {[1, 2, 3, 4, 5, 6].map((n) => (
        <div class={`node-card ${n === 1 ? 'management' : ''}`} style={`--delay: ${n * 0.1}s`}>
          <div class="node-icon">{n === 1 ? '👑' : '🖥️'}</div>
          <span class="node-name">Node {n}</span>
          <span class="node-model">{n === 1 ? 'Management' : 'SR630'}</span>
          <div class="node-pulse"></div>
        </div>
      ))}
    </div>

    <div class="cluster-network">
      <div class="network-layer">
        <span class="layer-badge fast">10G</span>
        <span class="layer-name">Management Network</span>
        <span class="layer-desc">Dedicated cluster communication</span>
      </div>
      <div class="network-layer">
        <span class="layer-badge">1G</span>
        <span class="layer-name">Production Network</span>
        <span class="layer-desc">LACP bonded (4x1G per node)</span>
      </div>
    </div>
  </div>

  <!-- Network Redesign -->
  <h2>// Network Architecture Redesign</h2>
  <p class="section-intro">
    The migration wasn't just a hypervisor swap. I redesigned the entire network architecture to eliminate
    bandwidth bottlenecks and segregate management traffic:
  </p>

  <div class="network-changes">
    <div class="change-card">
      <div class="change-header">
        <span class="change-icon">📡</span>
        <span class="change-title">10G PCIe NIC Installation</span>
      </div>
      <p>Physically installed dual-port 10 Gigabit PCIe network cards in all six servers. Opened chassis, verified PCIe slots, seated cards, ran cable infrastructure.</p>
    </div>

    <div class="change-card">
      <div class="change-header">
        <span class="change-icon">🔗</span>
        <span class="change-title">LACP Bonding (802.3ad)</span>
      </div>
      <p>Configured link aggregation across all onboard gigabit NICs. Four 1G ports bonded per server for redundancy and load balancing on production traffic.</p>
    </div>

    <div class="change-card">
      <div class="change-header">
        <span class="change-icon">🏷️</span>
        <span class="change-title">VLAN Segmentation</span>
      </div>
      <p>Implemented VLAN trunking across the infrastructure. Management, production, and lab traffic isolated on separate broadcast domains.</p>
    </div>

    <div class="change-card">
      <div class="change-header">
        <span class="change-icon">🌉</span>
        <span class="change-title">Linux Bridge Configuration</span>
      </div>
      <p>Configured virtual bridges for VM networking. Multiple bridge interfaces with VLAN tagging support for flexible lab topology deployment.</p>
    </div>

    <div class="change-card">
      <div class="change-header">
        <span class="change-icon">📋</span>
        <span class="change-title">Port Documentation</span>
      </div>
      <p>Created comprehensive port mapping documentation. Every cable run, switch port, and interface labeled and cataloged for future troubleshooting.</p>
    </div>

    <div class="change-card">
      <div class="change-header">
        <span class="change-icon">💾</span>
        <span class="change-title">Shared Storage Pools</span>
      </div>
      <p>Configured multiple shared storage pools accessible across all cluster nodes. Enables live migration and distributed VM placement.</p>
    </div>
  </div>

  <!-- Project Phases -->
  <h2>// Migration Phases</h2>
  <p class="section-intro">
    The project spanned several months with distinct phases. Each phase built on the previous; failures meant rebuilding:
  </p>

  <div class="phases-container">
    {phases.map((p) => (
      <div class="phase-card" style={`--phase: ${p.phase}`}>
        <div class="phase-header">
          <div class="phase-number">{p.phase}</div>
          <div class="phase-info">
            <span class="phase-title">{p.title}</span>
            <span class="phase-status complete">✓ Complete</span>
          </div>
        </div>
        <ul class="phase-tasks">
          {p.tasks.map((task) => (
            <li>{task}</li>
          ))}
        </ul>
      </div>
    ))}
  </div>

  <!-- NETLAB+ Integration -->
  <h2>// NETLAB+ Integration</h2>
  <p>
    The end goal was full integration with NDG's NETLAB+ platform for educational lab delivery. This involved
    restoring VM images from NDG's distribution system, configuring the NETLAB-VE management appliance,
    deploying lab pods (Cisco, Palo Alto, security courses), and provisioning student access. The Proxmox
    cluster now serves as the backbone for hands-on cybersecurity and networking education.
  </p>

  <!-- Lessons Learned -->
  <h2>// Lessons Learned (The Hard Way)</h2>
  <p class="section-intro">
    Building production infrastructure means breaking things. These are the moments that taught me the most:
  </p>

  <div class="lessons-container">
    <div class="lesson-card">
      <div class="lesson-header">
        <span class="lesson-icon">💥</span>
        <span class="lesson-title">The Proxmox 9 Incident</span>
      </div>
      <p>
        Upgraded the servers to Proxmox 9 and everything broke. That's when I learned the difference between
        Debian Trixie (13) and Bookworm (12). NETLAB+'s Proxmox packages were built for Bookworm; Trixie's
        libraries were incompatible. Lesson: always check upstream dependencies before major version upgrades.
        Rolled back and pinned to Proxmox 8.
      </p>
    </div>

    <div class="lesson-card">
      <div class="lesson-header">
        <span class="lesson-icon">🔄</span>
        <span class="lesson-title">Corosync Cluster Communication</span>
      </div>
      <p>
        Corosync is the heartbeat of a Proxmox cluster. Misconfigure it and nodes can't see each other;
        the cluster splits or loses quorum. Learning ring addresses, multicast vs unicast, and why the
        dedicated 10G network matters for cluster stability was critical. When Corosync breaks, nothing works.
      </p>
    </div>

    <div class="lesson-card">
      <div class="lesson-header">
        <span class="lesson-icon">🔗</span>
        <span class="lesson-title">Bonding vs Etherchannel</span>
      </div>
      <p>
        Linux bonding (802.3ad/LACP) and Cisco Etherchannel are the same concept but configured differently
        on each end. Getting the hash policy right, understanding how traffic distributes across bonded links,
        and debugging why one NIC was saturated while others sat idle taught me more about networking than
        any textbook.
      </p>
    </div>

    <div class="lesson-card">
      <div class="lesson-header">
        <span class="lesson-icon">🕸️</span>
        <span class="lesson-title">NIC Routing Across 6 Servers</span>
      </div>
      <p>
        Each server has multiple NICs: onboard gigabit ports, 10G PCIe cards, management interfaces. Keeping
        track of which physical port maps to which Linux interface, which bridge, which VLAN, across six
        servers requires meticulous documentation. One wrong cable and traffic routes to nowhere.
      </p>
    </div>
  </div>

  <!-- Skills Gained -->
  <h2>// Skills & Knowledge Gained</h2>
  <p class="section-intro">
    This project was a crash course in enterprise infrastructure. I went from VMware familiarity to deep
    Proxmox and Linux networking expertise:
  </p>

  <div class="skills-grid">
    {skillsGained.map((cat, i) => (
      <div class="skill-category" style={`--delay: ${i * 0.1}s`}>
        <h3 class="category-title">{cat.category}</h3>
        <div class="skill-items">
          {cat.items.map((item) => (
            <span class="skill-item">{item}</span>
          ))}
        </div>
      </div>
    ))}
  </div>

  <!-- Status -->
  <h2>// Project Status</h2>
  <div class="status-block">
    <div class="status-indicator completed">
      <div class="status-pulse"></div>
      <span>Migration complete and operational</span>
    </div>
    <ul class="status-list">
      <li><span class="check">✓</span> VMware dependency eliminated</li>
      <li><span class="check">✓</span> 6-node Proxmox cluster operational</li>
      <li><span class="check">✓</span> 10G management network deployed</li>
      <li><span class="check">✓</span> LACP bonding configured on all nodes</li>
      <li><span class="check">✓</span> NETLAB+ integration complete</li>
      <li><span class="check">✓</span> Lab pods deployed and accessible</li>
      <li><span class="check">✓</span> Student provisioning operational</li>
      <li><span class="check">✓</span> Documentation finalized</li>
    </ul>
  </div>

  <!-- Sources -->
  <div id="sources" class="sources-section">
    <h2>// Sources & References</h2>
    <p class="section-intro">
      The cost estimates on this page are based on publicly documented Broadcom VMware pricing (196 cores × $350/core/year = $68,600 annually).
      The following sources document VMware's licensing changes under Broadcom:
    </p>
    <div class="sources-list">
      {sources.map((source) => (
        <a href={source.url} target="_blank" rel="noopener noreferrer" class="source-link">
          <span class="source-icon">📄</span>
          <span class="source-title">{source.title}</span>
          <span class="source-arrow">→</span>
        </a>
      ))}
    </div>
  </div>
</ProjectLayout>

<style>
  /* Section Intro */
  .section-intro {
    opacity: 0.85;
    margin-bottom: 1.5rem;
  }

  /* Crisis Timeline */
  .crisis-timeline {
    display: flex;
    flex-direction: column;
    gap: 0;
    margin: 1.5rem 0;
    padding: 1.5rem;
    background: color-mix(in srgb, currentColor 3%, transparent);
    border: 1px solid color-mix(in srgb, currentColor 10%, transparent);
    border-radius: 8px;
  }

  .crisis-event {
    display: flex;
    align-items: flex-start;
    gap: 1rem;
    padding: 1rem 0;
  }

  .event-marker {
    width: 16px;
    height: 16px;
    border-radius: 50%;
    flex-shrink: 0;
    margin-top: 2px;
  }

  .event-marker.bad {
    background: #ef4444;
    box-shadow: 0 0 10px rgba(239, 68, 68, 0.5);
  }

  .event-marker.good {
    background: #10b981;
    box-shadow: 0 0 10px rgba(16, 185, 129, 0.5);
  }

  .event-content {
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
  }

  .event-date {
    font-family: "IBM Plex Mono", monospace;
    font-size: 0.75rem;
    font-weight: 600;
    opacity: 0.6;
  }

  .event-title {
    font-family: "IBM Plex Mono", monospace;
    font-size: 0.9rem;
    font-weight: 600;
  }

  .event-desc {
    font-size: 0.8rem;
    opacity: 0.7;
  }

  .crisis-connector {
    width: 2px;
    height: 20px;
    background: color-mix(in srgb, currentColor 15%, transparent);
    margin-left: 7px;
    position: relative;
    overflow: hidden;
  }

  .connector-particle {
    position: absolute;
    width: 2px;
    height: 8px;
    background: #ef4444;
    animation: flowDown 1s ease-in-out infinite;
  }

  @keyframes flowDown {
    0% { top: -8px; }
    100% { top: 20px; }
  }

  /* Cost Comparison */
  .cost-comparison {
    display: flex;
    align-items: stretch;
    gap: 1rem;
    margin: 1.5rem 0;
  }

  .cost-card {
    flex: 1;
    background: color-mix(in srgb, currentColor 3%, transparent);
    border: 1px solid color-mix(in srgb, currentColor 10%, transparent);
    border-radius: 8px;
    overflow: hidden;
  }

  .cost-card.vmware {
    border-top: 3px solid #ef4444;
  }

  .cost-card.proxmox {
    border-top: 3px solid #10b981;
  }

  .cost-header {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    padding: 1rem;
    background: color-mix(in srgb, currentColor 3%, transparent);
    border-bottom: 1px solid color-mix(in srgb, currentColor 10%, transparent);
  }

  .cost-icon {
    font-size: 1.25rem;
  }

  .cost-label {
    font-family: "IBM Plex Mono", monospace;
    font-size: 0.9rem;
    font-weight: 600;
  }

  .cost-body {
    padding: 1rem;
  }

  .cost-row {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 0.5rem 0;
    border-bottom: 1px dotted color-mix(in srgb, currentColor 10%, transparent);
  }

  .cost-row:last-child {
    border-bottom: none;
  }

  .cost-key {
    font-size: 0.8rem;
    opacity: 0.7;
  }

  .cost-value {
    font-family: "IBM Plex Mono", monospace;
    font-size: 0.85rem;
    font-weight: 600;
  }

  .cost-value.bad {
    color: #ef4444;
  }

  .cost-value.good {
    color: #10b981;
  }

  .cost-arrow {
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 0 0.5rem;
  }

  .arrow-savings {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.25rem;
    padding: 1rem;
    background: color-mix(in srgb, #10b981 10%, transparent);
    border: 1px solid color-mix(in srgb, #10b981 30%, transparent);
    border-radius: 8px;
  }

  .savings-label {
    font-family: "IBM Plex Mono", monospace;
    font-size: 0.6rem;
    letter-spacing: 0.1em;
    color: #10b981;
  }

  .savings-value {
    font-family: "IBM Plex Mono", monospace;
    font-size: 1.25rem;
    font-weight: 700;
    color: #10b981;
  }

  .savings-highlight {
    display: flex;
    justify-content: center;
    margin: 1rem 0 1.5rem;
  }

  .highlight-stat {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.25rem;
    padding: 1.5rem 3rem;
    background: color-mix(in srgb, #10b981 8%, transparent);
    border: 2px solid color-mix(in srgb, #10b981 30%, transparent);
    border-radius: 12px;
  }

  .highlight-stat .stat-value {
    font-family: "IBM Plex Mono", monospace;
    font-size: 1.75rem;
    font-weight: 700;
    color: #10b981;
  }

  .highlight-stat .stat-label {
    font-size: 0.85rem;
    opacity: 0.7;
  }

  .cite-link {
    font-family: "IBM Plex Mono", monospace;
    font-size: 0.7rem;
    margin-top: 0.5rem;
    opacity: 0.6;
    transition: opacity 0.2s ease;
  }

  .cite-link:hover {
    opacity: 1;
    background: transparent;
    color: #10b981;
  }

  /* Cluster Diagram */
  .cluster-diagram {
    background: color-mix(in srgb, currentColor 3%, transparent);
    border: 1px solid color-mix(in srgb, currentColor 10%, transparent);
    border-radius: 8px;
    padding: 1.5rem;
    margin: 1.5rem 0;
  }

  .cluster-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 1.5rem;
    padding-bottom: 1rem;
    border-bottom: 1px dashed color-mix(in srgb, currentColor 15%, transparent);
  }

  .cluster-name {
    font-family: "IBM Plex Mono", monospace;
    font-size: 1.1rem;
    font-weight: 600;
  }

  .cluster-status {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-family: "IBM Plex Mono", monospace;
    font-size: 0.8rem;
    color: #10b981;
  }

  .status-dot {
    width: 8px;
    height: 8px;
    background: #10b981;
    border-radius: 50%;
    animation: pulse 2s ease-in-out infinite;
  }

  @keyframes pulse {
    0%, 100% { opacity: 1; transform: scale(1); }
    50% { opacity: 0.5; transform: scale(0.8); }
  }

  .cluster-stats {
    display: flex;
    justify-content: center;
    gap: 3rem;
    margin-bottom: 1.5rem;
  }

  .cluster-stat {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.25rem;
  }

  .cluster-stat .stat-num {
    font-family: "IBM Plex Mono", monospace;
    font-size: 2rem;
    font-weight: 700;
    color: #10b981;
  }

  .cluster-stat .stat-label {
    font-size: 0.75rem;
    opacity: 0.6;
  }

  .cluster-nodes {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(100px, 1fr));
    gap: 1rem;
    margin-bottom: 1.5rem;
  }

  .node-card {
    position: relative;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.25rem;
    padding: 1rem;
    background: color-mix(in srgb, currentColor 5%, transparent);
    border: 1px solid color-mix(in srgb, currentColor 15%, transparent);
    border-radius: 6px;
    animation: fadeSlideIn 0.5s ease-out both;
    animation-delay: var(--delay);
  }

  @keyframes fadeSlideIn {
    from { opacity: 0; transform: translateY(10px); }
    to { opacity: 1; transform: translateY(0); }
  }

  .node-icon {
    font-size: 1.5rem;
  }

  .node-name {
    font-family: "IBM Plex Mono", monospace;
    font-size: 0.8rem;
    font-weight: 600;
  }

  .node-model {
    font-size: 0.65rem;
    opacity: 0.5;
  }

  .node-pulse {
    position: absolute;
    top: 6px;
    right: 6px;
    width: 6px;
    height: 6px;
    background: #10b981;
    border-radius: 50%;
    animation: pulse 2s ease-in-out infinite;
  }

  .cluster-network {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
  }

  .network-layer {
    flex: 1;
    min-width: 200px;
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
    padding: 1rem;
    background: color-mix(in srgb, currentColor 3%, transparent);
    border-radius: 6px;
  }

  .layer-badge {
    display: inline-block;
    width: fit-content;
    font-family: "IBM Plex Mono", monospace;
    font-size: 0.7rem;
    font-weight: 600;
    padding: 0.2rem 0.5rem;
    background: color-mix(in srgb, currentColor 10%, transparent);
    border-radius: 3px;
  }

  .layer-badge.fast {
    background: color-mix(in srgb, #10b981 20%, transparent);
    color: #10b981;
  }

  .layer-name {
    font-family: "IBM Plex Mono", monospace;
    font-size: 0.85rem;
    font-weight: 600;
  }

  .layer-desc {
    font-size: 0.75rem;
    opacity: 0.6;
  }

  /* Network Changes */
  .network-changes {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 1rem;
    margin: 1.5rem 0;
  }

  .change-card {
    background: color-mix(in srgb, currentColor 3%, transparent);
    border: 1px solid color-mix(in srgb, currentColor 10%, transparent);
    border-radius: 6px;
    padding: 1rem;
  }

  .change-header {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    margin-bottom: 0.75rem;
  }

  .change-icon {
    font-size: 1.25rem;
  }

  .change-title {
    font-family: "IBM Plex Mono", monospace;
    font-size: 0.85rem;
    font-weight: 600;
  }

  .change-card p {
    font-size: 0.8rem;
    opacity: 0.75;
    margin: 0;
    line-height: 1.5;
  }

  /* Phases */
  .phases-container {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 1rem;
    margin: 1.5rem 0;
  }

  .phase-card {
    background: color-mix(in srgb, currentColor 3%, transparent);
    border: 1px solid color-mix(in srgb, currentColor 10%, transparent);
    border-radius: 8px;
    overflow: hidden;
    animation: fadeSlideIn 0.5s ease-out both;
    animation-delay: calc(var(--phase) * 0.1s);
  }

  .phase-header {
    display: flex;
    align-items: center;
    gap: 1rem;
    padding: 1rem;
    background: color-mix(in srgb, currentColor 3%, transparent);
    border-bottom: 1px solid color-mix(in srgb, currentColor 10%, transparent);
  }

  .phase-number {
    width: 32px;
    height: 32px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: #10b981;
    color: white;
    border-radius: 6px;
    font-family: "IBM Plex Mono", monospace;
    font-size: 1rem;
    font-weight: 700;
  }

  .phase-info {
    display: flex;
    flex-direction: column;
    gap: 0.15rem;
  }

  .phase-title {
    font-family: "IBM Plex Mono", monospace;
    font-size: 0.85rem;
    font-weight: 600;
  }

  .phase-status {
    font-size: 0.7rem;
    color: #10b981;
  }

  .phase-tasks {
    list-style: none;
    padding: 1rem;
    margin: 0;
  }

  .phase-tasks li {
    font-size: 0.8rem;
    padding: 0.35rem 0;
    padding-left: 1rem;
    position: relative;
    opacity: 0.75;
  }

  .phase-tasks li::before {
    content: "•";
    position: absolute;
    left: 0;
    color: #10b981;
  }

  /* Lessons Learned */
  .lessons-container {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 1rem;
    margin: 1.5rem 0;
  }

  .lesson-card {
    background: color-mix(in srgb, currentColor 3%, transparent);
    border: 1px solid color-mix(in srgb, currentColor 10%, transparent);
    border-left: 3px solid #f59e0b;
    border-radius: 6px;
    padding: 1rem;
  }

  .lesson-header {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    margin-bottom: 0.75rem;
  }

  .lesson-icon {
    font-size: 1.25rem;
  }

  .lesson-title {
    font-family: "IBM Plex Mono", monospace;
    font-size: 0.85rem;
    font-weight: 600;
  }

  .lesson-card p {
    font-size: 0.8rem;
    opacity: 0.8;
    margin: 0;
    line-height: 1.6;
  }

  /* Skills Grid */
  .skills-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
    gap: 1rem;
    margin: 1.5rem 0;
  }

  .skill-category {
    background: color-mix(in srgb, currentColor 3%, transparent);
    border: 1px solid color-mix(in srgb, currentColor 10%, transparent);
    border-radius: 8px;
    padding: 1rem;
    animation: fadeSlideIn 0.5s ease-out both;
    animation-delay: var(--delay);
  }

  .category-title {
    font-family: "IBM Plex Mono", monospace;
    font-size: 0.85rem;
    font-weight: 600;
    color: #10b981;
    margin: 0 0 0.75rem 0;
  }

  .skill-items {
    display: flex;
    flex-wrap: wrap;
    gap: 0.4rem;
  }

  .skill-item {
    font-family: "IBM Plex Mono", monospace;
    font-size: 0.7rem;
    padding: 0.25rem 0.5rem;
    background: color-mix(in srgb, currentColor 5%, transparent);
    border: 1px solid color-mix(in srgb, currentColor 15%, transparent);
    border-radius: 3px;
    opacity: 0.8;
  }

  /* Status Block */
  .status-block {
    background: color-mix(in srgb, currentColor 3%, transparent);
    border: 1px solid color-mix(in srgb, currentColor 10%, transparent);
    border-radius: 8px;
    padding: 1.5rem;
    margin: 1.5rem 0;
  }

  .status-indicator {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    font-family: "IBM Plex Mono", monospace;
    font-size: 0.9rem;
    font-weight: 500;
    margin-bottom: 1rem;
  }

  .status-indicator.completed .status-pulse {
    background: #10b981;
    box-shadow: 0 0 10px rgba(16, 185, 129, 0.5);
  }

  .status-pulse {
    width: 12px;
    height: 12px;
    border-radius: 50%;
    animation: pulse 2s ease-in-out infinite;
  }

  .status-list {
    list-style: none;
    padding: 0;
    margin: 0;
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 0.5rem;
  }

  .status-list li {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 0.8rem;
    padding: 0.5rem 0.75rem;
    background: color-mix(in srgb, currentColor 3%, transparent);
    border-radius: 4px;
  }

  .check {
    color: #10b981;
    font-weight: bold;
  }

  /* Management Node */
  .node-card.management {
    border-color: #f59e0b;
    background: color-mix(in srgb, #f59e0b 8%, transparent);
  }

  .node-card.management .node-pulse {
    background: #f59e0b;
  }

  /* Sources */
  .sources-section {
    scroll-margin-top: 2rem;
  }

  .sources-list {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
    margin: 1.5rem 0;
  }

  .source-link {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    padding: 0.75rem 1rem;
    background: color-mix(in srgb, currentColor 3%, transparent);
    border: 1px solid color-mix(in srgb, currentColor 10%, transparent);
    border-radius: 6px;
    text-decoration: none;
    transition: all 0.2s ease;
    font-size: 0.8rem;
  }

  .source-link:hover {
    background: color-mix(in srgb, currentColor 8%, transparent);
    border-color: color-mix(in srgb, currentColor 20%, transparent);
    transform: translateX(4px);
  }

  .source-icon {
    font-size: 1rem;
    flex-shrink: 0;
  }

  .source-title {
    flex: 1;
    font-family: "IBM Plex Mono", monospace;
  }

  .source-arrow {
    opacity: 0.5;
    transition: opacity 0.2s ease;
  }

  .source-link:hover .source-arrow {
    opacity: 1;
  }

  /* Responsive */
  @media (max-width: 768px) {
    .cost-comparison {
      flex-direction: column;
    }

    .cost-arrow {
      padding: 1rem 0;
    }

    .cluster-stats {
      gap: 1.5rem;
    }

    .cluster-stat .stat-num {
      font-size: 1.5rem;
    }
  }
</style>
