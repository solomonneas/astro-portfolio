---
import ProjectLayout from "../../layouts/ProjectLayout.astro";

const frontmatter = {
  title: "NSET Hardware Inventory Management System",
  status: "Completed",
  statusType: "completed",
  origin: "Polk State College - BAS Capstone",
  date: "2025.05",
  techStack: ["PHP 7+", "MariaDB", "PDO", "bcrypt", "HTML/CSS", "JavaScript"],
};

// Stack components
const stackComponents = [
  { name: "PHP 7+", role: "Backend Logic", purpose: "Server-side authentication, CRUD operations, and session management with secure coding practices" },
  { name: "MariaDB 10.6", role: "Relational Database", purpose: "Normalized schema with foreign keys, ENUMs for categories/status, and unique serial number constraints" },
  { name: "PDO", role: "Database Abstraction", purpose: "Prepared statements for SQL injection prevention - modern replacement for deprecated mysql_* functions" },
  { name: "bcrypt", role: "Password Security", purpose: "PASSWORD_BCRYPT hashing with automatic salting - industry standard for secure credential storage" },
  { name: "PHP Sessions", role: "Authentication State", purpose: "Secure cookie-based sessions with HTTP-only, SameSite, and Secure flags" },
  { name: "Vanilla JavaScript", role: "Client Interactivity", purpose: "Popup windows and form handling without framework dependencies - demonstrating core language understanding" },
];

// Key features
const features = [
  { icon: "&#128274;", title: "Secure Authentication", description: "bcrypt password hashing with attempt throttling (max 5 failures), secure session cookies, and token-based password reset." },
  { icon: "&#128269;", title: "Advanced Search", description: "Multi-field search across name, category, serial, model, brand, location, and status. Dynamic filtering with WHERE 1=1 pattern." },
  { icon: "&#128221;", title: "Full CRUD Operations", description: "Create, Read, Update, Delete functionality with popup forms and parent window refresh on completion." },
  { icon: "&#128230;", title: "CSV Export", description: "Export filtered results to spreadsheet format. Respects current search/filter state for targeted exports." },
  { icon: "&#128101;", title: "Role-Based Access", description: "Admin and standard user roles with appropriate permission boundaries in the database schema." },
  { icon: "&#128205;", title: "37 Locations", description: "Predefined location taxonomy covering server rooms, labs, data centers, and office spaces." },
];

// Security implementations
const securityItems = [
  { vulnerability: "SQL Injection", mitigation: "PDO Prepared Statements", example: "$stmt->execute([$username])" },
  { vulnerability: "XSS", mitigation: "htmlspecialchars() output encoding", example: "htmlspecialchars($row['name'])" },
  { vulnerability: "Broken Auth", mitigation: "bcrypt + session security", example: "password_verify() + secure cookies" },
  { vulnerability: "Session Hijacking", mitigation: "HTTP-only, Secure, SameSite", example: "session_set_cookie_params()" },
  { vulnerability: "Brute Force", mitigation: "Login attempt throttling", example: "$_SESSION['login_attempts'] > 5" },
];

// Comparison data (Capstone vs LAIM)
const comparisonData = [
  { aspect: "Data Entry", capstone: "Manual form input", laim: "Auto-discovery from LibreNMS/Netdisco" },
  { aspect: "Updates", capstone: "User-initiated edits", laim: "Real-time network polling via SNMP" },
  { aspect: "Serial Numbers", capstone: "Manual entry", laim: "Automatic extraction from LLDP/CDP" },
  { aspect: "Scale", capstone: "~57 records", laim: "Thousands of devices" },
  { aspect: "Architecture", capstone: "Single PHP server (monolith)", laim: "Distributed microservices" },
  { aspect: "Authentication", capstone: "Session-based PHP", laim: "OAuth 2.0 / LDAP integration" },
];

// Skills demonstrated
const skills = [
  { name: "Full-Stack Development", items: ["PHP frontend + backend", "Complete CRUD architecture", "Authentication systems"] },
  { name: "Database Design", items: ["Normalized schema", "Foreign key constraints", "ENUM validation"] },
  { name: "Security Practices", items: ["OWASP Top 10 awareness", "bcrypt hashing", "Prepared statements"] },
  { name: "UI/UX Design", items: ["22 GUI mockups in Photoshop", "Consistent color scheme", "Functional layouts"] },
  { name: "Documentation", items: ["Design document", "ERD diagrams", "Presentation materials"] },
];
---

<ProjectLayout frontmatter={frontmatter}>
  <!-- Retro Visual Header -->
  <div class="retro-header">
    <div class="retro-window">
      <div class="window-titlebar">
        <span class="titlebar-text">NSET_Inventory.exe</span>
        <div class="titlebar-buttons">
          <span class="btn-minimize">_</span>
          <span class="btn-maximize">□</span>
          <span class="btn-close">×</span>
        </div>
      </div>
      <div class="window-content">
        <div class="terminal-box">
          <div class="terminal-title">NSET Hardware Inventory System v1.0</div>
          <div class="terminal-subtitle">Network System Engineering Technology</div>
          <div class="terminal-divider"></div>
          <div class="terminal-line"><span class="prompt">&gt;</span> Authenticated: solomon</div>
          <div class="terminal-line"><span class="prompt">&gt;</span> Records: 57</div>
          <div class="terminal-line"><span class="prompt">&gt;</span> Locations: 37</div>
          <div class="terminal-line"><span class="prompt">&gt;</span> Status: <span class="status-ok">OPERATIONAL</span></div>
        </div>
      </div>
    </div>
    <p class="retro-tagline">// Intentionally classic. Deliberately secure.</p>
  </div>

  <h2>// Overview</h2>
  <p>
    I built this hardware inventory management system as my capstone project for the Bachelor of Applied Science
    in Supervision and Management (Business Information Technology) at Polk State College. It's a complete CRUD
    web application designed for managing IT hardware assets in the Network System Engineering Technology department.
  </p>
  <p>
    The intentionally classic 2000s aesthetic is a deliberate design choice reflecting my web development roots
    starting in 1999. This project demonstrates that <strong>security and functionality don't require modern
    frameworks</strong>. The same secure coding principles (prepared statements, bcrypt hashing, session security)
    apply regardless of visual presentation.
  </p>

  <div class="context-callout">
    <div class="callout-header">
      <span class="callout-icon">&#128218;</span>
      <span class="callout-label">PORTFOLIO CONTEXT</span>
    </div>
    <p>
      This capstone represents <strong>foundational competency</strong> in full-stack development. The companion
      project <a href="/projects/laim">LAIM (Lab Asset Inventory Manager)</a> demonstrates how these fundamentals
      scale to enterprise solutions with network automation and real-time discovery.
    </p>
  </div>

  <!-- What It Does -->
  <h2>// What It Does</h2>
  <div class="features-grid">
    {features.map((feature) => (
      <div class="feature-card">
        <div class="feature-icon" set:html={feature.icon}></div>
        <div class="feature-content">
          <span class="feature-title">{feature.title}</span>
          <span class="feature-desc">{feature.description}</span>
        </div>
      </div>
    ))}
  </div>

  <!-- Tech Stack -->
  <h2>// Tech Stack</h2>
  <p class="section-comment">// Classic LAMP stack demonstrating server-side fundamentals</p>

  <div class="stack-grid">
    {stackComponents.map((comp, i) => (
      <div class="stack-card" style={`--delay: ${i * 0.1}s`}>
        <div class="stack-header">
          <span class="stack-indicator"></span>
          <span class="stack-name">{comp.name}</span>
        </div>
        <div class="stack-role">{comp.role}</div>
        <p class="stack-purpose">{comp.purpose}</p>
      </div>
    ))}
  </div>

  <!-- Architecture -->
  <h2>// System Architecture</h2>
  <div class="architecture-diagram">
    <div class="arch-layer">
      <span class="layer-label">AUTHENTICATION FLOW</span>
      <div class="flow-diagram">
        <div class="flow-step">
          <span class="step-icon">&#128100;</span>
          <span class="step-label">User Login</span>
        </div>
        <div class="flow-arrow">→</div>
        <div class="flow-step">
          <span class="step-icon">&#128274;</span>
          <span class="step-label">Check Attempts</span>
        </div>
        <div class="flow-arrow">→</div>
        <div class="flow-step">
          <span class="step-icon">&#128272;</span>
          <span class="step-label">bcrypt Verify</span>
        </div>
        <div class="flow-arrow">→</div>
        <div class="flow-step success">
          <span class="step-icon">&#9989;</span>
          <span class="step-label">Session Created</span>
        </div>
      </div>
    </div>

    <div class="arch-connector">
      <div class="connector-line"></div>
    </div>

    <div class="arch-layer">
      <span class="layer-label">FILE STRUCTURE</span>
      <div class="file-tree">
        <div class="file-item folder">Capstone/</div>
        <div class="file-item">├── index.php <span class="file-desc">// Login + inventory display</span></div>
        <div class="file-item">├── db.php <span class="file-desc">// PDO connection</span></div>
        <div class="file-item">├── add.php <span class="file-desc">// Add equipment popup</span></div>
        <div class="file-item">├── edit.php <span class="file-desc">// Edit equipment popup</span></div>
        <div class="file-item">├── update.php <span class="file-desc">// CRUD processor</span></div>
        <div class="file-item">├── delete.php <span class="file-desc">// Delete handler</span></div>
        <div class="file-item">├── export.php <span class="file-desc">// CSV export</span></div>
        <div class="file-item">├── register.php <span class="file-desc">// User registration</span></div>
        <div class="file-item">├── reset_password.php <span class="file-desc">// Token-based reset</span></div>
        <div class="file-item">└── logout.php <span class="file-desc">// Session termination</span></div>
      </div>
    </div>
  </div>

  <!-- Security Implementation -->
  <h2>// Security Implementation</h2>
  <p class="section-comment">// OWASP Top 10 awareness built into every layer</p>

  <div class="security-table">
    <div class="security-header">
      <span>Vulnerability</span>
      <span>Mitigation</span>
      <span>Implementation</span>
    </div>
    {securityItems.map((item) => (
      <div class="security-row">
        <span class="vuln-name">{item.vulnerability}</span>
        <span class="mitigation">{item.mitigation}</span>
        <code class="example">{item.example}</code>
      </div>
    ))}
  </div>

  <!-- Code Deep-Dive -->
  <h2>// How I Built It</h2>

  <div class="impl-section">
    <h3 class="impl-title">
      <span class="impl-num">01</span>
      Secure Login with Attempt Throttling
    </h3>
    <div class="impl-content">
      <div class="impl-block">
        <h4>Authentication Flow</h4>
        <div class="code-block">
          <code>session_start();</code>
          <code>include 'db.php';</code>
          <code></code>
          <code>// Secure session cookie settings</code>
          <code>session_set_cookie_params([</code>
          <code>    'secure' =&gt; true,</code>
          <code>    'httponly' =&gt; true,</code>
          <code>    'samesite' =&gt; 'Strict'</code>
          <code>]);</code>
          <code></code>
          <code>// Check login attempts (max 5)</code>
          <code>$_SESSION["login_attempts"] = ($_SESSION["login_attempts"] ?? 0) + 1;</code>
          <code>if ($_SESSION["login_attempts"] > 5) {</code>
          <code>    die("Too many login attempts.");</code>
          <code>}</code>
        </div>
      </div>
      <div class="impl-block">
        <h4>Password Verification</h4>
        <div class="code-block">
          <code>// Prepared statement prevents SQL injection</code>
          <code>$stmt = $pdo-&gt;prepare(</code>
          <code>    "SELECT id, password_hash FROM users WHERE username = ?"</code>
          <code>);</code>
          <code>$stmt-&gt;execute([$username]);</code>
          <code>$user = $stmt-&gt;fetch(PDO::FETCH_ASSOC);</code>
          <code></code>
          <code>// bcrypt comparison (timing-safe)</code>
          <code>if ($user && password_verify($password, $user["password_hash"])) {</code>
          <code>    $_SESSION["login_attempts"] = 0;  // Reset on success</code>
          <code>    $_SESSION["user_id"] = $user["id"];</code>
          <code>}</code>
        </div>
      </div>
    </div>
  </div>

  <div class="impl-section">
    <h3 class="impl-title">
      <span class="impl-num">02</span>
      Dynamic Multi-Field Search
    </h3>
    <div class="impl-content">
      <div class="impl-block full-width">
        <h4>WHERE 1=1 Pattern for Dynamic Filtering</h4>
        <div class="code-block">
          <code>// Base query allows appending conditions with AND</code>
          <code>$query = "SELECT * FROM hardware WHERE 1=1";</code>
          <code></code>
          <code>if (!empty($search)) {</code>
          <code>    $query .= " AND (name LIKE :search</code>
          <code>                    OR category LIKE :search</code>
          <code>                    OR serial_number LIKE :search</code>
          <code>                    OR model LIKE :search</code>
          <code>                    OR brand LIKE :search</code>
          <code>                    OR location LIKE :search)";</code>
          <code>}</code>
          <code></code>
          <code>if (!empty($selectedCategory)) {</code>
          <code>    $query .= " AND category = :category";</code>
          <code>}</code>
          <code></code>
          <code>// Named parameters prevent SQL injection</code>
          <code>$stmt-&gt;bindValue(':search', "%$search%", PDO::PARAM_STR);</code>
        </div>
      </div>
    </div>
    <p class="impl-note">
      The <code>WHERE 1=1</code> pattern is a classic technique for building dynamic queries.
      It allows any number of conditions to be appended with <code>AND</code> without worrying
      about whether it's the first condition or not.
    </p>
  </div>

  <div class="impl-section">
    <h3 class="impl-title">
      <span class="impl-num">03</span>
      Token-Based Password Reset
    </h3>
    <div class="impl-content">
      <div class="impl-block full-width">
        <h4>Single-Use Token Pattern</h4>
        <div class="code-block">
          <code>// Verify token exists in database</code>
          <code>$stmt = $pdo-&gt;prepare("SELECT id FROM users WHERE reset_token = ?");</code>
          <code>$stmt-&gt;execute([$token]);</code>
          <code>$user = $stmt-&gt;fetch(PDO::FETCH_ASSOC);</code>
          <code></code>
          <code>if ($user) {</code>
          <code>    // Hash new password with bcrypt (auto-salted)</code>
          <code>    $hashed = password_hash($new_password, PASSWORD_BCRYPT);</code>
          <code></code>
          <code>    // Update password AND invalidate token (single-use)</code>
          <code>    $stmt = $pdo-&gt;prepare(</code>
          <code>        "UPDATE users SET password_hash = ?, reset_token = NULL WHERE id = ?"</code>
          <code>    );</code>
          <code>    $stmt-&gt;execute([$hashed, $user['id']]);</code>
          <code>}</code>
        </div>
      </div>
    </div>
    <p class="impl-note">
      Setting <code>reset_token = NULL</code> after use ensures tokens can only be used once.
      Combined with bcrypt's automatic salting, each password gets a unique hash even if the
      plaintext is identical.
    </p>
  </div>

  <!-- Database Schema -->
  <h2>// Database Schema</h2>
  <div class="schema-diagram">
    <div class="table-card users">
      <div class="table-header">
        <span class="table-icon">&#128100;</span>
        <span class="table-name">users</span>
      </div>
      <div class="table-columns">
        <div class="column pk"><span class="key">PK</span> id INT</div>
        <div class="column">username VARCHAR(255)</div>
        <div class="column">password_hash VARCHAR(255)</div>
        <div class="column">role ENUM('admin','user')</div>
        <div class="column">created_at TIMESTAMP</div>
        <div class="column">reset_token VARCHAR(64)</div>
      </div>
    </div>
    <div class="schema-relation">
      <div class="relation-line"></div>
      <span class="relation-label">1:N</span>
    </div>
    <div class="table-card hardware">
      <div class="table-header">
        <span class="table-icon">&#128187;</span>
        <span class="table-name">hardware</span>
      </div>
      <div class="table-columns">
        <div class="column pk"><span class="key">PK</span> id INT</div>
        <div class="column">name VARCHAR(255)</div>
        <div class="column">category ENUM(12 types)</div>
        <div class="column unique">serial_number VARCHAR(255)</div>
        <div class="column">model, brand VARCHAR(255)</div>
        <div class="column">location VARCHAR(255)</div>
        <div class="column">status ENUM(4 states)</div>
        <div class="column fk"><span class="key">FK</span> checked_out_by INT</div>
      </div>
    </div>
  </div>

  <div class="schema-constraints">
    <div class="constraint-card">
      <span class="constraint-label">12 Categories</span>
      <span class="constraint-value">Switch, Firewall, Router, Monitor, Printer, Desktop PC, Laptop, Keyboard, Mouse, Cable, Adapter, Wireless AP</span>
    </div>
    <div class="constraint-card">
      <span class="constraint-label">4 Status Values</span>
      <span class="constraint-value">Available, In Use, Damaged, Retired</span>
    </div>
    <div class="constraint-card">
      <span class="constraint-label">37 Locations</span>
      <span class="constraint-value">Server Room, Labs 1-6, Data Center, various offices</span>
    </div>
  </div>

  <!-- Growth Comparison -->
  <h2>// Growth: Capstone → LAIM</h2>
  <p class="section-comment">// From manual CRUD to automated network discovery</p>

  <div class="comparison-visual">
    <div class="compare-side capstone-side">
      <div class="compare-header">
        <span class="compare-year">2025</span>
        <span class="compare-title">This Capstone</span>
      </div>
      <div class="compare-diagram">
        <div class="simple-box">
          <span class="box-label">PHP Server</span>
          <div class="box-content">
            <span>index.php</span>
            <span>update.php</span>
            <span>export.php</span>
          </div>
        </div>
        <div class="simple-arrow">↓</div>
        <div class="simple-box db">
          <span class="box-label">MariaDB</span>
          <span class="box-sub">2 tables</span>
        </div>
      </div>
    </div>

    <div class="evolution-arrow">
      <div class="arrow-line"></div>
      <span class="arrow-label">Evolution</span>
    </div>

    <div class="compare-side laim-side">
      <div class="compare-header">
        <span class="compare-year">2026</span>
        <span class="compare-title">LAIM (Modern)</span>
      </div>
      <div class="compare-diagram">
        <div class="distributed-stack">
          <div class="stack-item">LibreNMS (Monitoring)</div>
          <div class="stack-item">Netdisco (Discovery)</div>
          <div class="stack-item">PostgreSQL (Time-series)</div>
          <div class="stack-item">React Dashboard</div>
        </div>
      </div>
    </div>
  </div>

  <div class="comparison-table">
    <div class="compare-row header">
      <span>Aspect</span>
      <span>Capstone (2025)</span>
      <span>LAIM (Modern)</span>
    </div>
    {comparisonData.map((row) => (
      <div class="compare-row">
        <span class="aspect">{row.aspect}</span>
        <span class="capstone-val">{row.capstone}</span>
        <span class="laim-val">{row.laim}</span>
      </div>
    ))}
  </div>

  <div class="growth-takeaway">
    <p>
      <strong>What this evolution demonstrates:</strong> From manual to automated, from monolith to microservices,
      from SQL queries to network protocols (SNMP, LLDP, CDP), from forms to APIs. Both projects solve the same
      core problem (<em>tracking assets accurately</em>) but at vastly different scales and sophistication levels.
    </p>
  </div>

  <!-- Skills Demonstrated -->
  <h2>// Skills Demonstrated</h2>
  <div class="skills-grid">
    {skills.map((skill) => (
      <div class="skill-card">
        <div class="skill-header">
          <span class="skill-name">{skill.name}</span>
        </div>
        <ul class="skill-items">
          {skill.items.map((item) => (
            <li>{item}</li>
          ))}
        </ul>
      </div>
    ))}
  </div>

  <!-- Design Philosophy -->
  <h2>// Why Retro?</h2>
  <div class="philosophy-card">
    <div class="philosophy-content">
      <p>
        The intentional 2000s aesthetic isn't laziness; it's a statement. Table-based layouts, beveled buttons,
        gradient backgrounds, popup windows. This is how I learned to build the web starting in 1999.
      </p>
      <p>
        But look underneath: prepared statements prevent SQL injection. bcrypt hashes passwords with automatic
        salting. Session cookies are HTTP-only, Secure, and SameSite. Login attempts are throttled.
      </p>
      <p class="philosophy-punch">
        <strong>Modern security doesn't require modern styling.</strong> The principles are timeless.
      </p>
    </div>
  </div>

  <!-- Project Status -->
  <h2>// Project Status</h2>
  <div class="status-block">
    <div class="status-indicator completed">
      <div class="status-pulse"></div>
      <span>Capstone presented May 2025 | Grade: 100% | Bachelor's degree earned</span>
    </div>
    <ul class="status-list">
      <li><span class="check">&#10003;</span> Full CRUD functionality implemented</li>
      <li><span class="check">&#10003;</span> Secure authentication with bcrypt</li>
      <li><span class="check">&#10003;</span> SQL injection prevention via prepared statements</li>
      <li><span class="check">&#10003;</span> Multi-field search with dynamic filtering</li>
      <li><span class="check">&#10003;</span> CSV export with filter state preservation</li>
      <li><span class="check">&#10003;</span> Token-based password reset</li>
      <li><span class="check">&#10003;</span> 22 GUI mockups in design document</li>
      <li><span class="check">&#10003;</span> BAS Capstone requirement fulfilled</li>
      <li><span class="check">&#10003;</span> Graduated with Bachelor of Applied Science</li>
    </ul>
  </div>
</ProjectLayout>

<style>
  /* Retro Header Visual */
  .retro-header {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1rem;
    margin-bottom: 1.5rem;
  }

  .retro-window {
    width: 100%;
    max-width: 400px;
    background: color-mix(in srgb, currentColor 5%, transparent);
    border: 2px solid color-mix(in srgb, currentColor 20%, transparent);
    border-radius: 4px;
    overflow: hidden;
  }

  .window-titlebar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 0.25rem 0.5rem;
    background: color-mix(in srgb, currentColor 10%, transparent);
    border-bottom: 1px solid color-mix(in srgb, currentColor 15%, transparent);
  }

  .titlebar-text {
    font-family: "IBM Plex Mono", monospace;
    font-size: 0.75rem;
    font-weight: 600;
  }

  .titlebar-buttons {
    display: flex;
    gap: 0.25rem;
  }

  .titlebar-buttons span {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 16px;
    height: 16px;
    font-size: 0.7rem;
    background: color-mix(in srgb, currentColor 5%, transparent);
    border: 1px solid color-mix(in srgb, currentColor 15%, transparent);
    border-radius: 2px;
    cursor: default;
  }

  .window-content {
    padding: 0.75rem;
  }

  .terminal-box {
    font-family: "IBM Plex Mono", monospace;
    font-size: 0.7rem;
    line-height: 1.5;
    color: #10b981;
    padding: 0.5rem;
    border: 1px solid color-mix(in srgb, #10b981 40%, transparent);
    border-radius: 4px;
    background: color-mix(in srgb, #10b981 5%, transparent);
  }

  .terminal-title {
    font-weight: 600;
    text-align: center;
  }

  .terminal-subtitle {
    text-align: center;
    opacity: 0.8;
    margin-bottom: 0.5rem;
  }

  .terminal-divider {
    height: 1px;
    background: color-mix(in srgb, #10b981 40%, transparent);
    margin: 0.5rem 0;
  }

  .terminal-line {
    opacity: 0.9;
  }

  .terminal-line .prompt {
    opacity: 0.6;
    margin-right: 0.25rem;
  }

  .terminal-line .status-ok {
    font-weight: 600;
  }

  .retro-tagline {
    font-family: "IBM Plex Mono", monospace;
    font-size: 0.8rem;
    opacity: 0.6;
    margin: 0;
  }

  /* Context Callout */
  .context-callout {
    margin: 1.5rem 0;
    padding: 1rem;
    background: color-mix(in srgb, currentColor 3%, transparent);
    border: 1px solid color-mix(in srgb, currentColor 10%, transparent);
    border-left: 3px solid #10b981;
    border-radius: 4px;
  }

  .callout-header {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    margin-bottom: 0.5rem;
  }

  .callout-icon {
    font-size: 1rem;
  }

  .callout-label {
    font-family: "IBM Plex Mono", monospace;
    font-size: 0.7rem;
    font-weight: 700;
    letter-spacing: 0.1em;
    color: #10b981;
  }

  .context-callout p {
    font-size: 0.85rem;
    line-height: 1.6;
    margin: 0;
    opacity: 0.85;
  }

  .context-callout a {
    color: #10b981;
    text-decoration: none;
    border-bottom: 1px solid color-mix(in srgb, #10b981 40%, transparent);
  }

  .context-callout a:hover {
    border-bottom-color: #10b981;
  }

  /* Section Comment */
  .section-comment {
    font-family: "IBM Plex Mono", monospace;
    font-size: 0.8rem;
    opacity: 0.6;
    margin-bottom: 1.5rem;
  }

  /* Features Grid */
  .features-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 1rem;
    margin: 1.5rem 0;
  }

  .feature-card {
    display: flex;
    gap: 1rem;
    padding: 1rem;
    background: color-mix(in srgb, currentColor 3%, transparent);
    border: 1px solid color-mix(in srgb, currentColor 10%, transparent);
    border-radius: 6px;
  }

  .feature-icon {
    font-size: 1.75rem;
    opacity: 0.8;
    flex-shrink: 0;
  }

  .feature-content {
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
  }

  .feature-title {
    font-family: "IBM Plex Mono", monospace;
    font-size: 0.85rem;
    font-weight: 600;
  }

  .feature-desc {
    font-size: 0.75rem;
    opacity: 0.75;
    line-height: 1.5;
  }

  /* Stack Grid */
  .stack-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 1rem;
    margin: 1.5rem 0;
  }

  .stack-card {
    background: color-mix(in srgb, currentColor 3%, transparent);
    border: 1px solid color-mix(in srgb, currentColor 10%, transparent);
    border-left: 3px solid #10b981;
    border-radius: 4px;
    padding: 1rem;
    animation: fadeSlideIn 0.5s ease-out both;
    animation-delay: var(--delay);
  }

  @keyframes fadeSlideIn {
    from { opacity: 0; transform: translateY(10px); }
    to { opacity: 1; transform: translateY(0); }
  }

  .stack-header {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    margin-bottom: 0.5rem;
  }

  .stack-indicator {
    width: 8px;
    height: 8px;
    background: #10b981;
    border-radius: 50%;
    animation: pulse 2s ease-in-out infinite;
  }

  @keyframes pulse {
    0%, 100% { opacity: 1; transform: scale(1); }
    50% { opacity: 0.5; transform: scale(0.8); }
  }

  .stack-name {
    font-family: "IBM Plex Mono", monospace;
    font-size: 1rem;
    font-weight: 600;
  }

  .stack-role {
    font-family: "IBM Plex Mono", monospace;
    font-size: 0.75rem;
    color: #10b981;
    margin-bottom: 0.75rem;
  }

  .stack-purpose {
    font-size: 0.8rem;
    opacity: 0.75;
    margin: 0;
    line-height: 1.5;
  }

  /* Architecture Diagram */
  .architecture-diagram {
    background: color-mix(in srgb, currentColor 3%, transparent);
    border: 1px solid color-mix(in srgb, currentColor 10%, transparent);
    border-radius: 8px;
    padding: 1.5rem;
    margin: 1.5rem 0;
  }

  .arch-layer {
    margin-bottom: 1rem;
  }

  .layer-label {
    display: block;
    font-family: "IBM Plex Mono", monospace;
    font-size: 0.65rem;
    letter-spacing: 0.15em;
    opacity: 0.5;
    text-transform: uppercase;
    margin-bottom: 1rem;
    text-align: center;
  }

  .flow-diagram {
    display: flex;
    align-items: center;
    justify-content: center;
    flex-wrap: wrap;
    gap: 0.5rem;
  }

  .flow-step {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.25rem;
    padding: 0.75rem 1rem;
    background: color-mix(in srgb, currentColor 5%, transparent);
    border: 1px solid color-mix(in srgb, currentColor 15%, transparent);
    border-radius: 6px;
    min-width: 100px;
  }

  .flow-step.success {
    border-color: color-mix(in srgb, #10b981 40%, transparent);
    background: color-mix(in srgb, #10b981 8%, transparent);
  }

  .step-icon {
    font-size: 1.25rem;
  }

  .step-label {
    font-family: "IBM Plex Mono", monospace;
    font-size: 0.7rem;
    text-align: center;
  }

  .flow-arrow {
    font-size: 1.25rem;
    color: #10b981;
    opacity: 0.6;
  }

  .arch-connector {
    display: flex;
    justify-content: center;
    padding: 0.5rem 0;
  }

  .connector-line {
    width: 2px;
    height: 20px;
    background: color-mix(in srgb, currentColor 20%, transparent);
  }

  .file-tree {
    background: color-mix(in srgb, currentColor 3%, transparent);
    border: 1px solid color-mix(in srgb, currentColor 10%, transparent);
    border-radius: 4px;
    padding: 1rem;
    max-width: 500px;
    margin: 0 auto;
  }

  .file-item {
    font-family: "IBM Plex Mono", monospace;
    font-size: 0.75rem;
    padding: 0.25rem 0;
    opacity: 0.85;
  }

  .file-item.folder {
    font-weight: 600;
    color: #f59e0b;
  }

  .file-desc {
    opacity: 0.5;
    font-size: 0.7rem;
  }

  /* Security Table */
  .security-table {
    background: color-mix(in srgb, currentColor 3%, transparent);
    border: 1px solid color-mix(in srgb, currentColor 10%, transparent);
    border-radius: 8px;
    overflow: hidden;
    margin: 1.5rem 0;
  }

  .security-header {
    display: grid;
    grid-template-columns: 1fr 1.5fr 1.5fr;
    padding: 0.75rem 1rem;
    background: color-mix(in srgb, currentColor 5%, transparent);
    border-bottom: 1px solid color-mix(in srgb, currentColor 10%, transparent);
    font-family: "IBM Plex Mono", monospace;
    font-size: 0.75rem;
    font-weight: 600;
  }

  .security-row {
    display: grid;
    grid-template-columns: 1fr 1.5fr 1.5fr;
    padding: 0.75rem 1rem;
    border-bottom: 1px solid color-mix(in srgb, currentColor 5%, transparent);
    font-size: 0.8rem;
    align-items: center;
  }

  .security-row:last-child {
    border-bottom: none;
  }

  .vuln-name {
    font-family: "IBM Plex Mono", monospace;
    font-weight: 500;
    color: #ef4444;
  }

  .mitigation {
    opacity: 0.85;
  }

  .security-row code.example {
    font-family: "IBM Plex Mono", monospace;
    font-size: 0.7rem;
    background: color-mix(in srgb, currentColor 5%, transparent);
    padding: 0.25rem 0.5rem;
    border-radius: 3px;
    color: #10b981;
  }

  /* Implementation Sections */
  .impl-section {
    background: color-mix(in srgb, currentColor 3%, transparent);
    border: 1px solid color-mix(in srgb, currentColor 10%, transparent);
    border-radius: 8px;
    padding: 1.5rem;
    margin: 1rem 0;
  }

  .impl-title {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    font-family: "IBM Plex Mono", monospace;
    font-size: 1rem;
    font-weight: 600;
    margin: 0 0 1rem 0;
    color: currentColor;
  }

  .impl-num {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 28px;
    height: 28px;
    background: color-mix(in srgb, #10b981 15%, transparent);
    border: 1px solid #10b981;
    border-radius: 50%;
    font-size: 0.75rem;
    color: #10b981;
  }

  .impl-content {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 1rem;
  }

  .impl-block h4 {
    font-family: "IBM Plex Mono", monospace;
    font-size: 0.8rem;
    color: #10b981;
    margin: 0 0 0.5rem 0;
  }

  .impl-block.full-width {
    grid-column: 1 / -1;
  }

  .code-block {
    background: color-mix(in srgb, currentColor 5%, transparent);
    border: 1px solid color-mix(in srgb, currentColor 10%, transparent);
    border-radius: 4px;
    padding: 0.75rem;
    overflow-x: auto;
  }

  .code-block code {
    display: block;
    font-family: "IBM Plex Mono", monospace;
    font-size: 0.7rem;
    line-height: 1.6;
    opacity: 0.85;
  }

  .code-block code:empty {
    height: 0.5rem;
  }

  .impl-note {
    font-size: 0.8rem;
    opacity: 0.75;
    margin: 1rem 0 0 0;
    padding: 0.75rem;
    background: color-mix(in srgb, currentColor 3%, transparent);
    border-left: 2px solid #10b981;
    line-height: 1.6;
  }

  .impl-note code {
    background: color-mix(in srgb, currentColor 8%, transparent);
    padding: 0.1rem 0.3rem;
    border-radius: 2px;
    font-size: 0.75rem;
  }

  /* Schema Diagram */
  .schema-diagram {
    display: flex;
    align-items: center;
    justify-content: center;
    flex-wrap: wrap;
    gap: 1.5rem;
    margin: 1.5rem 0;
    padding: 1.5rem;
    background: color-mix(in srgb, currentColor 3%, transparent);
    border: 1px solid color-mix(in srgb, currentColor 10%, transparent);
    border-radius: 8px;
  }

  .table-card {
    background: color-mix(in srgb, currentColor 3%, transparent);
    border: 1px solid color-mix(in srgb, currentColor 15%, transparent);
    border-radius: 6px;
    overflow: hidden;
    min-width: 200px;
  }

  .table-header {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.5rem 0.75rem;
    background: color-mix(in srgb, currentColor 5%, transparent);
    border-bottom: 1px solid color-mix(in srgb, currentColor 10%, transparent);
  }

  .table-icon {
    font-size: 1rem;
  }

  .table-name {
    font-family: "IBM Plex Mono", monospace;
    font-size: 0.85rem;
    font-weight: 600;
  }

  .table-columns {
    padding: 0.5rem 0.75rem;
  }

  .column {
    font-family: "IBM Plex Mono", monospace;
    font-size: 0.7rem;
    padding: 0.25rem 0;
    opacity: 0.8;
  }

  .column.pk { color: #f59e0b; }
  .column.fk { color: #3b82f6; }
  .column.unique { font-style: italic; }

  .key {
    font-size: 0.6rem;
    padding: 0.1rem 0.25rem;
    background: color-mix(in srgb, currentColor 10%, transparent);
    border-radius: 2px;
    margin-right: 0.25rem;
  }

  .schema-relation {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.25rem;
  }

  .relation-line {
    width: 40px;
    height: 2px;
    background: color-mix(in srgb, currentColor 30%, transparent);
  }

  .relation-label {
    font-family: "IBM Plex Mono", monospace;
    font-size: 0.65rem;
    opacity: 0.6;
  }

  .schema-constraints {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 1rem;
    margin: 1rem 0;
  }

  .constraint-card {
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
    padding: 0.75rem;
    background: color-mix(in srgb, currentColor 3%, transparent);
    border: 1px solid color-mix(in srgb, currentColor 10%, transparent);
    border-radius: 4px;
  }

  .constraint-label {
    font-family: "IBM Plex Mono", monospace;
    font-size: 0.75rem;
    font-weight: 600;
    color: #10b981;
  }

  .constraint-value {
    font-size: 0.75rem;
    opacity: 0.75;
    line-height: 1.5;
  }

  /* Comparison Visual */
  .comparison-visual {
    display: flex;
    align-items: stretch;
    justify-content: center;
    gap: 1rem;
    margin: 1.5rem 0;
    flex-wrap: wrap;
  }

  .compare-side {
    flex: 1;
    min-width: 200px;
    max-width: 280px;
    background: color-mix(in srgb, currentColor 3%, transparent);
    border: 1px solid color-mix(in srgb, currentColor 10%, transparent);
    border-radius: 8px;
    overflow: hidden;
  }

  .compare-header {
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
    padding: 0.75rem;
    background: color-mix(in srgb, currentColor 5%, transparent);
    border-bottom: 1px solid color-mix(in srgb, currentColor 10%, transparent);
    text-align: center;
  }

  .compare-year {
    font-family: "IBM Plex Mono", monospace;
    font-size: 0.7rem;
    opacity: 0.6;
  }

  .compare-title {
    font-family: "IBM Plex Mono", monospace;
    font-size: 0.9rem;
    font-weight: 600;
  }

  .capstone-side .compare-header { border-top: 3px solid #f59e0b; }
  .laim-side .compare-header { border-top: 3px solid #10b981; }

  .compare-diagram {
    padding: 1rem;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.5rem;
  }

  .simple-box {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.25rem;
    padding: 0.75rem;
    background: color-mix(in srgb, currentColor 5%, transparent);
    border: 1px solid color-mix(in srgb, currentColor 15%, transparent);
    border-radius: 4px;
    width: 100%;
  }

  .simple-box.db {
    background: color-mix(in srgb, #3b82f6 8%, transparent);
    border-color: color-mix(in srgb, #3b82f6 30%, transparent);
  }

  .box-label {
    font-family: "IBM Plex Mono", monospace;
    font-size: 0.75rem;
    font-weight: 600;
  }

  .box-content {
    display: flex;
    flex-direction: column;
    font-size: 0.65rem;
    opacity: 0.7;
    text-align: center;
  }

  .box-sub {
    font-size: 0.65rem;
    opacity: 0.6;
  }

  .simple-arrow {
    font-size: 1rem;
    color: #10b981;
    opacity: 0.5;
  }

  .distributed-stack {
    display: flex;
    flex-direction: column;
    gap: 0.35rem;
    width: 100%;
  }

  .stack-item {
    padding: 0.4rem 0.6rem;
    background: color-mix(in srgb, #10b981 8%, transparent);
    border: 1px solid color-mix(in srgb, #10b981 25%, transparent);
    border-radius: 3px;
    font-size: 0.7rem;
    text-align: center;
  }

  .evolution-arrow {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 0.25rem;
  }

  .arrow-line {
    width: 40px;
    height: 3px;
    background: linear-gradient(90deg, #f59e0b, #10b981);
    border-radius: 2px;
  }

  .arrow-label {
    font-family: "IBM Plex Mono", monospace;
    font-size: 0.65rem;
    opacity: 0.6;
  }

  /* Comparison Table */
  .comparison-table {
    background: color-mix(in srgb, currentColor 3%, transparent);
    border: 1px solid color-mix(in srgb, currentColor 10%, transparent);
    border-radius: 8px;
    overflow: hidden;
    margin: 1.5rem 0;
  }

  .compare-row {
    display: grid;
    grid-template-columns: 1fr 1.5fr 1.5fr;
    padding: 0.75rem 1rem;
    border-bottom: 1px solid color-mix(in srgb, currentColor 5%, transparent);
    font-size: 0.8rem;
  }

  .compare-row:last-child {
    border-bottom: none;
  }

  .compare-row.header {
    background: color-mix(in srgb, currentColor 5%, transparent);
    border-bottom: 1px solid color-mix(in srgb, currentColor 10%, transparent);
    font-family: "IBM Plex Mono", monospace;
    font-weight: 600;
    font-size: 0.75rem;
  }

  .aspect {
    font-family: "IBM Plex Mono", monospace;
    font-weight: 500;
  }

  .capstone-val {
    opacity: 0.7;
  }

  .laim-val {
    color: #10b981;
  }

  .growth-takeaway {
    padding: 1rem;
    background: color-mix(in srgb, currentColor 3%, transparent);
    border: 1px solid color-mix(in srgb, currentColor 10%, transparent);
    border-left: 3px solid #10b981;
    border-radius: 4px;
    margin: 1.5rem 0;
  }

  .growth-takeaway p {
    font-size: 0.85rem;
    line-height: 1.6;
    margin: 0;
    opacity: 0.85;
  }

  /* Skills Grid */
  .skills-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 1rem;
    margin: 1.5rem 0;
  }

  .skill-card {
    background: color-mix(in srgb, currentColor 3%, transparent);
    border: 1px solid color-mix(in srgb, currentColor 10%, transparent);
    border-radius: 6px;
    overflow: hidden;
  }

  .skill-header {
    padding: 0.75rem 1rem;
    background: color-mix(in srgb, currentColor 3%, transparent);
    border-bottom: 1px solid color-mix(in srgb, currentColor 8%, transparent);
  }

  .skill-name {
    font-family: "IBM Plex Mono", monospace;
    font-size: 0.85rem;
    font-weight: 600;
  }

  .skill-items {
    list-style: none;
    padding: 0.75rem 1rem;
    margin: 0;
  }

  .skill-items li {
    font-size: 0.75rem;
    padding: 0.3rem 0;
    padding-left: 1rem;
    position: relative;
    opacity: 0.75;
  }

  .skill-items li::before {
    content: ">";
    position: absolute;
    left: 0;
    color: #10b981;
    font-family: "IBM Plex Mono", monospace;
  }

  /* Philosophy Card */
  .philosophy-card {
    background: color-mix(in srgb, currentColor 3%, transparent);
    border: 1px solid color-mix(in srgb, currentColor 10%, transparent);
    border-radius: 8px;
    padding: 1.5rem;
    margin: 1.5rem 0;
  }

  .philosophy-content p {
    font-size: 0.9rem;
    line-height: 1.7;
    margin: 0 0 1rem 0;
    opacity: 0.85;
  }

  .philosophy-content p:last-child {
    margin-bottom: 0;
  }

  .philosophy-punch {
    padding: 0.75rem;
    background: color-mix(in srgb, #10b981 8%, transparent);
    border-left: 3px solid #10b981;
    border-radius: 4px;
  }

  /* Status Block */
  .status-block {
    background: color-mix(in srgb, currentColor 3%, transparent);
    border: 1px solid color-mix(in srgb, currentColor 10%, transparent);
    border-radius: 8px;
    padding: 1.5rem;
    margin: 1.5rem 0;
  }

  .status-indicator {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    font-family: "IBM Plex Mono", monospace;
    font-size: 0.9rem;
    font-weight: 500;
    margin-bottom: 1rem;
  }

  .status-indicator.completed .status-pulse {
    background: #10b981;
    box-shadow: 0 0 10px rgba(16, 185, 129, 0.5);
  }

  .status-pulse {
    width: 12px;
    height: 12px;
    border-radius: 50%;
    animation: pulse 2s ease-in-out infinite;
  }

  .status-list {
    list-style: none;
    padding: 0;
    margin: 0;
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 0.5rem;
  }

  .status-list li {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 0.8rem;
    padding: 0.5rem 0.75rem;
    background: color-mix(in srgb, currentColor 3%, transparent);
    border-radius: 4px;
  }

  .check {
    color: #10b981;
    font-weight: bold;
  }

  /* Responsive */
  @media (max-width: 768px) {
    .security-header,
    .security-row,
    .compare-row {
      grid-template-columns: 1fr;
      gap: 0.25rem;
    }

    .security-header span:not(:first-child),
    .compare-row.header span:not(:first-child) {
      display: none;
    }

    .security-row {
      padding: 1rem;
    }

    .comparison-visual {
      flex-direction: column;
      align-items: center;
    }

    .compare-side {
      max-width: 100%;
    }

    .evolution-arrow {
      transform: rotate(90deg);
    }

    .flow-diagram {
      flex-direction: column;
    }

    .flow-arrow {
      transform: rotate(90deg);
    }

    .schema-diagram {
      flex-direction: column;
    }

    .schema-relation {
      transform: rotate(90deg);
    }

    .impl-content {
      grid-template-columns: 1fr;
    }
  }
</style>
