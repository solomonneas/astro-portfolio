---
import Layout from "../layouts/Layout.astro";
import StatusLight from "../components/StatusLight.astro";
---

<Layout pageTitle="Home">
  <section class="readme-terminal">
    <header class="terminal-header">
      <div class="header-left">
        <StatusLight status="ongoing" />
        <span class="status-text">ONLINE</span>
      </div>
      <h1>$ cat ~/README.md</h1>
    </header>

    <div class="terminal-body">
      <!-- Intro Section -->
      <div class="readme-section intro">
        <p class="greeting"># Hello, I'm Solomon Neas</p>
        <p class="tagline">
          Father | Network & Systems Engineer | AI Engineer | Educator | Software Developer | Cybersecurity Graduate Student
        </p>
        <p class="bio">
          I build production infrastructure designed to teach. Open-source SOC, NOC, and virtualization
          platforms with documentation baked in so students can study, break, and rebuild real systems.
          ~$165K+ in annual cost avoidance for Polk State College. Pursuing M.S. in Cybersecurity
          Intelligence and Information Security at USF.
        </p>
      </div>

      <!-- Quick Stats -->
      <div class="stats-row">
        <div class="stat">
          <span class="stat-label">[LOCATION]</span>
          <span class="stat-value">Florida, USA</span>
        </div>
        <div class="stat">
          <span class="stat-label">[STATUS]</span>
          <span class="stat-value status-available">Available for Work</span>
        </div>
        <div class="stat">
          <span class="stat-label">[FOCUS]</span>
          <span class="stat-value">Infrastructure, Security, AI & Programming</span>
        </div>
      </div>

      <!-- Directory Listing -->
      <div class="directory-section">
        <p class="section-header">## ~/sitemap</p>
        <p class="section-comment">// Navigate to learn more</p>

        <div class="directory-grid">
          <!-- About -->
          <a href="/about" class="dir-card">
            <div class="dir-header">
              <span class="dir-icon">drwxr-xr-x</span>
              <span class="dir-name">/about</span>
            </div>
            <p class="dir-desc">Background, skills, and certifications. The full picture.</p>
            <span class="dir-link">$ cd /about</span>
          </a>

          <!-- Experience -->
          <a href="/experience" class="dir-card">
            <div class="dir-header">
              <span class="dir-icon">drwxr-xr-x</span>
              <span class="dir-name">/experience</span>
            </div>
            <p class="dir-desc">Current roles, education timeline, and professional history.</p>
            <span class="dir-link">$ cd /experience</span>
          </a>

          <!-- Projects -->
          <a href="/projects" class="dir-card">
            <div class="dir-header">
              <span class="dir-icon">drwxr-xr-x</span>
              <span class="dir-name">/projects</span>
            </div>
            <p class="dir-desc">SOC builds, homelab infrastructure, migrations, and research.</p>
            <span class="dir-link">$ cd /projects</span>
          </a>

          <!-- Blog -->
          <a href="/blog" class="dir-card">
            <div class="dir-header">
              <span class="dir-icon">drwxr-xr-x</span>
              <span class="dir-name">/blog</span>
            </div>
            <p class="dir-desc">Thoughts on networking, security, education, and tech.</p>
            <span class="dir-link">$ cd /blog</span>
          </a>

          <!-- Hire -->
          <a href="/hire" class="dir-card highlight">
            <div class="dir-header">
              <span class="dir-icon">-rwxr-xr-x</span>
              <span class="dir-name">/hire</span>
              <StatusLight status="ongoing" class="card-status" />
            </div>
            <p class="dir-desc">Consulting, adjunct teaching, and web development services.</p>
            <span class="dir-link">$ cd /hire</span>
          </a>

          <!-- Contact -->
          <a href="/contact" class="dir-card">
            <div class="dir-header">
              <span class="dir-icon">-rw-r--r--</span>
              <span class="dir-name">/contact</span>
            </div>
            <p class="dir-desc">Email, LinkedIn, GitHub, and other ways to connect.</p>
            <span class="dir-link">$ cd /contact</span>
          </a>
        </div>
      </div>

      <!-- Footer -->
      <div class="terminal-footer">
        <span class="cursor-blink">█</span> EOF
      </div>
    </div>
  </section>
</Layout>

<style>
  .readme-terminal {
    font-family: "IBM Plex Mono", monospace;
    max-width: 52rem;
    margin: 0 auto;
  }

  .terminal-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 1rem 1.25rem;
    background: color-mix(in srgb, currentColor 5%, transparent);
    border: 1px solid color-mix(in srgb, currentColor 15%, transparent);
    border-bottom: none;
    border-radius: 6px 6px 0 0;
  }

  .header-left {
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }

  .status-text {
    font-size: 0.7rem;
    font-weight: 600;
    letter-spacing: 0.1em;
    color: #10b981;
  }

  .terminal-header h1 {
    font-size: 1rem;
    font-weight: 500;
    margin: 0;
    opacity: 0.8;
  }

  .terminal-body {
    padding: 1.5rem 1.25rem 1.25rem;
    border: 1px solid color-mix(in srgb, currentColor 15%, transparent);
    border-top: none;
    border-radius: 0 0 6px 6px;
    background: color-mix(in srgb, currentColor 2%, transparent);
  }

  /* Intro Section */
  .intro {
    margin-bottom: 2rem;
  }

  .greeting {
    font-size: 1.5rem;
    font-weight: 700;
    margin: 0 0 0.5rem 0;
    color: #10b981;
  }

  .tagline {
    font-size: 0.8rem;
    opacity: 0.7;
    margin: 0 0 1rem 0;
    letter-spacing: 0.02em;
  }

  .bio {
    font-size: 0.9rem;
    line-height: 1.6;
    margin: 0;
    max-width: 50ch;
    opacity: 0.85;
  }

  /* Stats Row */
  .stats-row {
    display: flex;
    flex-wrap: wrap;
    gap: 1rem;
    margin-bottom: 2.5rem;
    padding: 1rem;
    background: color-mix(in srgb, currentColor 3%, transparent);
    border: 1px dashed color-mix(in srgb, currentColor 15%, transparent);
    border-radius: 4px;
  }

  .stat {
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
  }

  .stat-label {
    font-size: 0.65rem;
    opacity: 0.5;
    letter-spacing: 0.05em;
  }

  .stat-value {
    font-size: 0.8rem;
    font-weight: 500;
  }

  .stat-value.status-available {
    color: #10b981;
  }

  /* Directory Section */
  .directory-section {
    margin-bottom: 1.5rem;
  }

  .section-header {
    font-size: 1.1rem;
    font-weight: 600;
    margin: 0 0 0.25rem 0;
    opacity: 0.9;
  }

  .section-comment {
    font-size: 0.75rem;
    opacity: 0.5;
    margin: 0 0 1.25rem 0;
    font-style: italic;
  }

  .directory-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
    gap: 1rem;
  }

  .dir-card {
    display: flex;
    flex-direction: column;
    padding: 1rem;
    background: color-mix(in srgb, currentColor 3%, transparent);
    border: 1px solid color-mix(in srgb, currentColor 10%, transparent);
    border-radius: 4px;
    text-decoration: none;
    color: inherit;
    transition: all 0.2s ease;
  }

  .dir-card:hover {
    background: color-mix(in srgb, currentColor 7%, transparent);
    border-color: color-mix(in srgb, currentColor 25%, transparent);
    transform: translateY(-2px);
  }

  .dir-card.highlight {
    border-color: color-mix(in srgb, #10b981 30%, transparent);
  }

  .dir-card.highlight:hover {
    border-color: #10b981;
    box-shadow: 0 0 15px color-mix(in srgb, #10b981 15%, transparent);
  }

  .dir-header {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    margin-bottom: 0.5rem;
  }

  .dir-icon {
    font-size: 0.65rem;
    opacity: 0.4;
  }

  .dir-name {
    font-size: 0.9rem;
    font-weight: 600;
    color: #10b981;
  }

  .card-status {
    margin-left: auto;
  }

  .dir-desc {
    font-size: 0.75rem;
    opacity: 0.7;
    margin: 0 0 0.75rem 0;
    line-height: 1.5;
    flex-grow: 1;
  }

  .dir-link {
    font-size: 0.7rem;
    opacity: 0.5;
    transition: opacity 0.2s ease, color 0.2s ease;
  }

  .dir-card:hover .dir-link {
    opacity: 1;
    color: #10b981;
  }

  /* Footer */
  .terminal-footer {
    margin-top: 1rem;
    font-size: 0.8rem;
    opacity: 0.5;
  }

  .cursor-blink {
    animation: blink 1s step-end infinite;
  }

  @keyframes blink {
    from, to { opacity: 1; }
    50% { opacity: 0; }
  }

  /* Responsive */
  @media (max-width: 640px) {
    .terminal-header {
      flex-direction: column;
      align-items: flex-start;
      gap: 0.75rem;
    }

    .stats-row {
      flex-direction: column;
    }

    .directory-grid {
      grid-template-columns: 1fr;
    }

    .greeting {
      font-size: 1.25rem;
    }
  }
</style>
